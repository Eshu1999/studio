
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the DocConnect application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity. This should be the Firebase Auth UID."
        },
        "username": {
          "type": "string",
          "description": "The user's unique username."
        },
        "firstName": {
          "type": "string",
          "description": "First name of the user."
        },
        "lastName": {
          "type": "string",
          "description": "Last name of the user."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "role": {
          "type": "string",
          "description": "The role of the user within the application.",
          "enum": ["patient", "doctor", "admin"]
        },
        "verificationStatus": {
          "type": "string",
          "description": "The verification status for a doctor account.",
          "enum": ["pending", "verified", "rejected"]
        }
      },
      "required": [
        "id",
        "email",
        "role"
      ]
    },
    "DoctorProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "DoctorProfile",
      "type": "object",
      "description": "Represents the professional profile of a user with the 'doctor' role.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the DoctorProfile, typically the same as the User UID."
        },
        "fullName": {
            "type": "string",
            "description": "The doctor's full legal name."
        },
        "stateOfRegistration": {
            "type": "string",
            "description": "The state or jurisdiction where the doctor is registered to practice."
        },
        "medicalCouncilId": {
            "type": "string",
            "description": "The doctor's official ID from their medical council."
        },
        "licenseDocument": {
            "type": "string",
            "description": "Path or URL to the uploaded license document in Firebase Storage."
        },
        "manualVerificationRequired": {
            "type": "boolean",
            "description": "Set by an admin to indicate the doctor needs a manual video call verification."
        },
        "specialization": {
          "type": "string",
          "description": "The doctor's area of specialization (e.g., Cardiology, Pediatrics)."
        },
        "experienceYears": {
          "type": "number",
          "description": "The number of years of experience the doctor has."
        },
        "availability": {
          "type": "string",
          "description": "Information about the doctor's availability (e.g., days of the week, time slots)."
        },
        "photoUrl": {
          "type": "string",
          "description": "URL to the doctor's profile photo.",
          "format": "uri"
        }
      },
      "required": [
        "id", "fullName", "stateOfRegistration", "medicalCouncilId", "licenseDocument"
      ]
    },
    "PatientProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "PatientProfile",
      "type": "object",
      "description": "Represents the profile of a user with the 'patient' role.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the PatientProfile, typically the same as the User UID."
        },
        "dateOfBirth": {
          "type": "string",
          "format": "date",
          "description": "The patient's date of birth."
        },
        "medicalHistorySummary": {
          "type": "string",
          "description": "A brief summary of the patient's medical history."
        }
      },
      "required": ["id"]
    },
    "Appointment": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Appointment",
      "type": "object",
      "description": "Represents a scheduled appointment between a user and a doctor.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Appointment entity."
        },
        "patientId": {
          "type": "string",
          "description": "Reference to the User with role 'patient'."
        },
        "doctorId": {
          "type": "string",
          "description": "Reference to the User with role 'doctor'."
        },
        "startTime": {
          "type": "string",
          "description": "The date and time the appointment is scheduled to begin.",
          "format": "date-time"
        },
        "endTime": {
          "type": "string",
          "description": "The date and time the appointment is scheduled to end.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The status of the appointment such as 'Scheduled', 'Completed', or 'Cancelled'."
        }
      },
      "required": [
        "id",
        "patientId",
        "doctorId",
        "startTime",
        "endTime",
        "status"
      ]
    },
    "MedicalRecord": {
      "title": "MedicalRecord",
      "type": "object",
      "description": "Represents a patient's comprehensive medical record, compiled from various interactions.",
      "properties": {
        "patientId": {
          "type": "string",
          "description": "The UID of the patient this record belongs to."
        },
        "appointments": {
          "type": "array",
          "items": { "$ref": "#/backend/entities/Appointment" },
          "description": "A list of all appointments for the patient."
        },
        "summaries": {
          "type": "array",
          "items": { "$ref": "#/backend/entities/ConsultationSummary" },
          "description": "A list of all consultation summaries for the patient."
        }
      },
      "required": ["patientId"]
    },
    "ConsultationSummary": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ConsultationSummary",
      "type": "object",
      "description": "Represents the summary of a consultation generated after a video call.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ConsultationSummary entity."
        },
        "appointmentId": {
          "type": "string",
          "description": "Reference to Appointment. (Relationship: Appointment 1:1 ConsultationSummary)"
        },
        "summaryText": {
          "type": "string",
          "description": "The text of the generated consultation summary."
        },
        "createdTime": {
          "type": "string",
          "description": "The time at which the consultation summary was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "appointmentId",
        "summaryText",
        "createdTime"
      ]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores all user profiles, for both patients and doctors. The 'role' field distinguishes them. Path-based ownership ensures users can only access their own profile data.",
          "security": {
            "read": "request.auth.uid == userId",
            "write": "request.auth.uid == userId"
          },
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/doctorProfile/{profileId}",
        "definition": {
          "entityName": "DoctorProfile",
          "schema": {
            "$ref": "#/backend/entities/DoctorProfile"
          },
          "description": "Stores the professional profile for users with the 'doctor' role. Only the doctor themselves can read or write to this document.",
          "security": {
            "read": "request.auth.uid == userId",
            "write": "request.auth.uid == userId"
          },
          "params": [
            {
              "name": "userId",
              "description": "The UID of the user this profile belongs to."
            },
            {
              "name": "profileId",
              "description": "The unique identifier for the doctor profile, should be the same as the userId."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/patientProfile/{profileId}",
        "definition": {
          "entityName": "PatientProfile",
          "schema": { "$ref": "#/backend/entities/PatientProfile"},
          "description": "Stores the profile for users with the 'patient' role."
        }
      },
      {
        "path": "/appointments/{appointmentId}",
        "definition": {
          "entityName": "Appointment",
          "schema": {
            "$ref": "#/backend/entities/Appointment"
          },
          "description": "Top-level collection for all appointments. Security rules will grant access based on patientId or doctorId.",
          "params": [
            {
              "name": "appointmentId",
              "description": "The unique identifier for the appointment."
            }
          ]
        }
      },
      {
        "path": "/appointments/{appointmentId}/consultationSummary/{summaryId}",
        "definition": {
          "entityName": "ConsultationSummary",
          "schema": {
            "$ref": "#/backend/entities/ConsultationSummary"
          },
          "description": "Stores consultation summaries for a given appointment.",
          "params": [
            {
              "name": "appointmentId",
              "description": "The ID of the parent appointment."
            },
            {
              "name": "summaryId",
              "description": "The unique ID for the summary."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to support a role-based application (patients and doctors). A single `/users` collection is used for authentication and basic profile data, with a `role` field to differentiate. Doctor-specific professional data is stored in a subcollection `/users/{userId}/doctorProfile` to keep the main user document lean. Appointments are in a top-level collection for easier querying by both patients and doctors, with security rules ensuring that only involved parties can access the appointment details. This structure is scalable and secure."
  }
}
